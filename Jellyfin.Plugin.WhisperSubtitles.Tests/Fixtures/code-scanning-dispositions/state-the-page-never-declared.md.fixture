# A register using a state it never declared

`set aside` is a rule read to its end and accepted as it stands.

`repair owed` is a rule whose pattern is to be changed in this tree.

## cs/example-first, set aside

Accepted for the fixture.

## cs/example-second, noted

"Noted" says nothing about what was decided, and no paragraph above defines it.
